// Operadores Aritméticos em JavaScript

let soma = 5 + 2;        // Adição (+)
let subtracao = 5 - 2;   // Subtração (-)
let multiplicacao = 5 * 2; // Multiplicação (*)
let divisao = 5 / 2;     // Divisão Real (/)
let resto = 5 % 2;       // Resto da Divisão Inteira (%)
let potencia = 5 ** 2;    // Potência (**)